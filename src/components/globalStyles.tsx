import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  heading: {
    fontSize: 30,
    fontWeight: "bold",
  },
  author: {
    fontSize: 15,
    opacity: 0.5,
  },
});

export default styles;
